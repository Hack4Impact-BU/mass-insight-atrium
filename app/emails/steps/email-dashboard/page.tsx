"use client"
import React from "react";

// Email dashboard will be the first and last page of sending an email

const Page: React.FC = () => {

return (
    <div>
        <p>Email dashboard</p>
    </div>
    );
};

export default Page;